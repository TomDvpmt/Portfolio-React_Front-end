import { createTheme } from "@mui/material";

const theme = createTheme({
    typography: {
        h1: {
            fontSize: "3rem",
            textAlign: "center",
        },
    },
});

export default theme;
